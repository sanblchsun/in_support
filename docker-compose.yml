version: '3.1'

services:

#    network_mode: "hostchat"

  tgbot:
    container_name: bot_chat_type_anydesk
    build: .
    volumes:
      - .:/src
    command: python app.py
    restart: always
    network_mode: "bridge"

    env_file:
      - ".env"
